# PDF → JPEG 변환 웹앱 개발 프롬프트 (Google Antigravity용)

아래 내용을 그대로 복사해서 Google Antigravity 에이전트에게 전달하세요.

---

## 프롬프트

```
PDF 파일을 JPEG 이미지로 변환해주는 웹앱을 만들어줘.

## 핵심 기능
1. 사용자가 PDF 파일을 업로드한다 (드래그 앤 드롭 + 파일 선택 버튼 모두 지원).
2. 업로드된 PDF의 각 페이지를 JPEG 이미지로 변환한다.
3. 변환 전에 페이지 수, 파일명, 썸네일 미리보기를 보여준다.
4. 변환 옵션을 제공한다:
   - 이미지 품질 (0~100, 슬라이더)
   - 해상도/DPI (예: 72 / 150 / 300 DPI 선택)
   - 변환할 페이지 범위 지정 (전체 / 특정 페이지 / 범위, 예: 1-3,5)
5. 변환이 끝나면:
   - 각 페이지 결과를 썸네일 그리드로 보여준다.
   - 개별 페이지를 JPEG로 다운로드할 수 있다.
   - 여러 페이지를 한 번에 ZIP으로 묶어 다운로드할 수 있다.
6. 변환 진행 상태를 progress bar로 표시한다 (페이지별 진행률).
7. 여러 PDF 파일을 동시에 업로드해서 순차적으로 처리할 수 있게 한다 (선택 사항, 우선순위 낮음).

## 처리 방식
- 가능하면 **클라이언트 사이드(브라우저 내)**에서 PDF → 이미지 변환을 처리해줘.
  - PDF 렌더링은 pdf.js(PDF.js) 라이브러리를 사용하고, canvas에 그린 후 canvas.toBlob()으로 JPEG를 추출해.
  - 이렇게 하면 서버로 파일을 업로드할 필요가 없어서 개인정보 보호에도 유리하고 서버 비용도 안 들어.
- 서버 사이드 처리가 필요하다고 판단되면(예: 대용량 파일, 고해상도 배치 변환) 그 이유와 함께 백엔드 아키텍처를 제안하고, 어떤 방식을 선택했는지 알려줘.
- ZIP 묶음 다운로드는 클라이언트에서 JSZip 같은 라이브러리로 처리해줘.

## 기술 스택 (선호사항, 강제 아님 — 더 적합한 대안 있으면 제안해줘)
- 프론트엔드: React + TypeScript + Vite
- 스타일링: Tailwind CSS
- PDF 렌더링: pdf.js
- ZIP 생성: jszip + file-saver

## UI/UX 요구사항
- 반응형 디자인 (모바일/데스크톱 모두 자연스럽게 동작)
- 다크모드 지원
- 드래그 앤 드롭 영역에 명확한 시각적 피드백 (hover, active 상태)
- 에러 처리: PDF가 아닌 파일 업로드, 손상된 PDF, 용량 초과(예: 100MB 이상) 시 사용자에게 명확한 에러 메시지 표시
- 변환 중 취소 버튼 제공
- 접근성: 키보드 네비게이션, 스크린리더용 aria-label 적용

## 성능/제약사항
- 대용량 PDF(수백 페이지) 처리 시 브라우저가 멈추지 않도록 Web Worker 사용을 고려해줘.
- 페이지별로 점진적으로 렌더링해서 사용자가 진행 상황을 즉시 볼 수 있게 해줘.

## 산출물
- 실행 가능한 전체 프로젝트 코드
- README에 실행 방법(설치, 개발 서버 실행, 빌드) 작성
- 주요 컴포넌트 구조에 대한 간단한 설명

먼저 전체 아키텍처와 폴더 구조를 제안하고, 내가 승인하면 구현을 시작해줘.
```

---

## 사용 팁
- Google Antigravity는 **Gemini 3** 기반 에이전틱 IDE로, 계획 수립(Planning) 후 코드를 생성하는 방식이 잘 동작합니다. 위 프롬프트의 마지막 줄("먼저 전체 아키텍처와... 승인하면 구현을 시작해줘")처럼 **계획 단계를 먼저 요청**하면 결과물의 방향을 검토하고 수정 요청하기 쉬워집니다.
- 브라우저 자동화(Playwright 통합)를 지원하므로, 구현 후 "브라우저에서 실제로 PDF를 업로드해서 변환 결과를 확인해줘"라고 추가 요청하면 직접 테스트까지 맡길 수 있습니다.
- 큰 기능 단위로 나눠서 반복 요청하는 것도 좋습니다: (1) 업로드+미리보기 → (2) 변환 로직 → (3) 다운로드/ZIP → (4) UI 다듬기/에러 처리.
